import * as actions from '../actions/cheese';

const intital = {
  cheeses: [],
  loading: false,
  error: null
}

export const cheeseReducer = function(state = intital, action) {

  if (action)
  }
}
